{
  "extends": "@fxhash/tsconfig/base",
  "include": ["src/**/*"],
  "exclude": ["node_modules"],
  "compilerOptions": {
    "rootDir": "src",
    "outDir": "dist",
    "lib": ["dom"],
    // TODO: Decide if we want to lower the target for better compatibility
    // Current sdk is released with es2022 target
    "target": "es2022"
  }
}
