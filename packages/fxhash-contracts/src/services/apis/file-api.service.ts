import { config } from "@fxhash/config"

const ROOT = config.PI_FILE_ROOT

// URL to endpoint to upload random file
export const API_FILE__ARTICLE_UPLOAD_FILE = `${ROOT}/article/file`
export const API_FILE__ARTICLE_UPLOAD_METADATA = `${ROOT}/article/metadata`
