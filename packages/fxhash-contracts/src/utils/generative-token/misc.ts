import { BlockchainType } from "@/services/operations/ContractOperation"
import { GenerativeToken } from "@/types/entities/GenerativeToken"

export const getChainFromToken = (token: GenerativeToken) => {
  // TODO: implement
  return BlockchainType.TEZOS
}
