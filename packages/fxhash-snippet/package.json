{
  "name": "@fxhash/fxhash-snippet",
  "version": "2.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run clean && tsc --declaration && tsc-alias",
    "build:watch": "npm run clean && tsc && (concurrently \"tsc -w --declaration\" \"tsc-alias -w\")",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "node-html-parser": "^6.1.5"
  },
  "devDependencies": {}
}
